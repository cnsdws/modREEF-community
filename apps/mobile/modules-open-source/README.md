# Open-source native modules

This intentionally empty native-module directory is selected when Tuya
provisioning is disabled. It prevents Expo Autolinking from pulling the local
Tuya bridge and its separately licensed native dependencies into a standard
open-source build.
